- Format tables cleanly with proper alignment
- Use professional legal terminology appropriate for patent analysis`


// V2: Compact Stage 4 prompt aligned to the Stage 4 UI.
// Focuses on a small, JSON-only output without perâ€‘patent tables to reduce tokens.
export const NOVELTY_REPORT_PROMPT_V2 = `You are a senior patent attorney preparing the FINAL Stage 4 novelty report for the Novelty UI.

Use the following inputs only for reasoning â€” do not repeat or dump them back:
- invention_features: {invention_features}
- selected_patents: {selected_patents}
- search_metadata: {search_metadata}
- feature_analysis_matrix: {feature_analysis_matrix}
- structured_narrative: {structured_narrative}

Goal:
- Produce a concise executive-level summary and focused concluding remarks aligned with the Stage 4 UI.
- Keep output minimal; avoid large tables and perâ€‘patent writeups (these are handled by the app/PDF generator).

Strict instructions:
- Do NOT include: tables, table_of_contents, perâ€‘patent sections, citations, hyperlinks, CPC codes, or copied abstracts.
- Do NOT restate numeric matrices or lists already provided in inputs.
- Ground the summary in the feature_analysis_matrix and selected_patents; do not invent metrics.
- Keep the executive summary â‰¤ 300 words. Keep each bullet â‰¤ 18 words.
- Return valid JSON only with the exact shape below. No prose or backticks.

Output JSON shape:
{
  "report_metadata": {
    "title": "Novelty Assessment Report",
    "search_id": "SEARCH_ID",
    "date": "GENERATION_DATE",
    "analyst": "Kisho of PatentNest.ai"
  },
  "executive_summary": {
    "summary": "One tight paragraph on why novelty exists, the role of combinations, and next focus."
  },
  "concluding_remarks": {
    "overall_novelty_assessment": "Novel | Partially Novel | Not Novel",
    "key_strengths": ["...", "...", "..."],
    "key_risks": ["...", "..."],
    "strategic_recommendations": ["...", "...", "..."],
    "filing_advice": "Actionable single sentence"
  },
  "structured_narrative": {
    "integration": "Prefer reusing the provided narrative; compress to 1â€“2 sentences.",
    "feature_insights": "Prefer reusing the provided narrative; compress to 1â€“2 sentences.",
    "verdict": "Prefer reusing the provided narrative; compress to 1 sentence."
  }
}
`;

// Lightweight prompt: compile final report strictly from per-patent remarks
export const NOVELTY_REPORT_FROM_REMARKS_PROMPT = `You are preparing a concise novelty assessment report.

INPUTS:
- invention_features: JSON array of mechanism-level features (strings)
- per_patent_remarks: JSON array where each item is {"pn","remarks","overlap_features","missing_features","novelty_points","confidence","title","decision"}.
- metrics: {"novelty_score":number, "decision": "Novel|Partially Novel|Not Novel|Low Evidence", "confidence":"High|Medium|Low"}

INSTRUCTIONS:
- Use ONLY the per_patent_remarks array as narrative evidence. Do not restate abstracts, tables, or raw mappings.
- Output clean JSON; no markdown fences or extra text.
- Keep conclusions crisp and defensible; no legal language.

OUTPUT JSON SHAPE:
{
  "executive_summary": {
    "summary": "2-3 sentences summarizing overall novelty and rationale",
    "novelty_score": "NN.N%",
    "confidence": "High|Medium|Low",
    "visual_cards": {"Novelty Score":"..%","Patents Analyzed":"N","Unique Features":"X of Y","Confidence":"..."}
  },
  "concluding_remarks": {
    "key_strengths": ["up to 3 bullets"],
    "key_risks": ["up to 3 bullets"],
    "strategic_recommendations": ["2-3 actions"],
    "advisory": "1 sentence cautionary advisory",
    "per_patent_discussion": [{"patent_number":"PN","novelty_overlap":"1 sentence","gaps_or_differences":"1 sentence"}]
  }
}`;

export interface NoveltySearchConfig {
  jurisdiction: string;
  filingType: string;
  tenantId?: string;
  // Stage 1.5 - AI Relevance gate on PQAI results
  stage15: {
    modelPreference: 'gemini-2.5-flash-lite' | 'gemini-2.5-pro' | 'gemini-2.0-flash-lite' | 'gpt-4o-mini' | 'gpt-4o' | 'claude-2.5';
    thresholds: { high: number; medium: number };
    borderlineQuota: number; // number of borderline items to keep for diversity/recall
    maxCandidates: number;   // upper bound of PQAI items to gate
    batchSize: number;       // batch size for LLM calls
  };
  stage0: {
    customPrompt?: string;
    extractionRules?: Record<string, any>;
  };
  stage1: {
    maxPatents: number;
    relevanceThresholds: { high: number; medium: number };
    customPrompt?: string;
  };
  stage35a: {
    batchSize: number;
    maxRefsTotal: number;
    thresholdPresent: number;
    thresholdPartial: number;
    criticalFeatures: string[];
    modelPreference: 'gpt-4o' | 'gpt-4o-mini' | 'claude-2.5' | 'gemini-2.0-flash-lite' | 'gemini-2.5-flash-lite' | 'gemini-2.5-pro';
    // When Stage 1.5 accepts more than the 50% quota, allow extra mapping capacity
    acceptedOverflowRatio?: number;   // default 0.15 (15% of total PQAI)
    borderlineOverflowRatio?: number; // default 0.10 (10% of total PQAI)
    customPrompt?: string;
  };
  stage35b: {
    // Deterministic - no config needed
  };
  stage35c?: {
    modelPreference: 'gpt-4o' | 'gpt-4o-mini' | 'claude-2.5' | 'gemini-2.0-flash-lite' | 'gemini-2.5-flash-lite' | 'gemini-2.5-pro';
    maxPatentsForRemarks?: number; // Optional cap; default: all in feature_map
